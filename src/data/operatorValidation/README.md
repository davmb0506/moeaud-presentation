# Datos — validación operadores (exp02)

JSON independiente del bloque **MOEA-UD / VEGF-A** (`bindingValidation.json`).

| Archivo | Uso |
|---------|-----|
| `operatorValidation.json` | Campeones base/both/temp: cascade, HADDOCK, PRODIGY, head-to-head |

Regenerar desde EvoPro_Mod:

```bash
python scripts/build_operator_validation.py
```

Fuente: `EvoPro_Mod/evopro/validation/operator_experiment/`
